# User Input Required

These tasks require macOS privacy prompts or physical input that cannot be completed safely by the agent alone.

Already verified without additional user input:

- `./script/smoke_packet_fixture.sh` passed on June 4, 2026 after live Chrome Tab fixture support was added. Latest successful packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780553877-57E09280-15FC-4F58-9737-9F120480D954`; partial failure packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/partial-failure-fixture-1780553917-F7FA5417-5BE9-4D65-A3DE-A477944D3BEE`; paused packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/paused-fixture-1780553917-02CC8A3B-8631-4AA1-90BB-6F9E78D99A33`; OCR fixture packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/ocr-fixture-1780553877-0EFE141B-187F-42E8-AD43-4257D3AED292`.
- `./script/live_capture_fixture.sh chromeTab --duration 1` passed on June 4, 2026 against a blank Google Chrome tab without AI processing. Packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/live-capture-fixtures/live-chromeTab-fixture-1780553828-255D6723-1C86-4308-9957-A9CA7E41496B`; status `partial`; verification `passed`; `raw/capture-session.json` records `mode=chromeTab`, `appName=Google Chrome`, `chromeTab.url=about:blank`, `chromeTab.windowIndex=1`, `chromeTab.tabIndex=1`, and a resolved window ID. `ffprobe` verified H.264 video and AAC audio.
- The latest smoke verified `semantic-segments.json` and `semantic-timeline.md` are generated, `manifest.json` records `files.semanticSegments=semantic-segments.json` and `files.semanticTimeline=semantic-timeline.md`, `agent-prompt.md` includes `## Semantic Timeline`, the default zip includes both semantic files, and segments are ordered, non-overlapping, titled, sourced, summarized, and linked to selected frame files.
- The latest smoke verified `project-context.md` is generated, `manifest.json` records `files.projectContext=project-context.md`, `agent-prompt.md` includes `## Project Context`, the default zip includes `project-context.md`, and fixture secret/heavy content such as `SYN_SHOULD_NOT_APPEAR` / `node_modules/ignored.txt` is excluded.
- `SYN_UI_SHOW_SETTINGS=1 ./script/capture_syn_ui.sh build/ui-captures/syn-settings-project-context-2026-06-04.png` passed on June 4, 2026. The photograb shows the Settings Project Context section with `Choose Folder` and `Clear`; the status sidecar reports Microphone, Screen Recording, and Accessibility all granted.
- `./script/smoke_packet_fixture.sh` passed on June 4, 2026 after Smart Region verification was added. Latest successful packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780548940-BE3AAB02-28B6-4686-977E-783B1BC8A9E2`; partial failure packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/partial-failure-fixture-1780548983-5C6EE2AC-FC60-4DFE-B965-4EFF8C9D2502`; paused packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/paused-fixture-1780548983-E73236D8-2A33-47A4-BC42-F484785AA771`; OCR fixture packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/ocr-fixture-1780548939-73523C3F-1A2C-448B-BF0E-583961655B14`.
- `./script/smoke_packet_fixture.sh` passed on June 4, 2026 after non-destructive video trim verification was added. Latest successful packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780550940-146CE238-4422-476B-BA65-AC429EB1EEDA`; partial failure packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/partial-failure-fixture-1780550982-8C590BC5-6BA4-43A6-89AA-71456622F9EC`; paused packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/paused-fixture-1780550982-FC27C35F-9746-4318-BAEC-78CE22B6BBA0`; OCR fixture packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/ocr-fixture-1780550939-B6849973-5EE9-44FB-B739-198F678EE969`.
- `./script/smoke_packet_fixture.sh` passed on June 4, 2026 after compact packet verification was added. Latest successful packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780551582-34A8E530-F15C-403D-B0A2-A7A46D586D8E`; partial failure packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/partial-failure-fixture-1780551627-A855B53C-562D-48C1-963C-34CE681E8078`; paused packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/paused-fixture-1780551627-EA29F060-02B2-4866-92BD-C1CCCA3048EC`; OCR fixture packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/ocr-fixture-1780551581-097FD3BC-E1F1-4ED7-B99C-71400AFBAA92`.
- The latest smoke output included `SYN_COMPACT_ZIP_INCLUDES_AGENT_FILES=yes` and `SYN_COMPACT_ZIP_EXCLUDES_HEAVY_FILES=yes`, proving compact zips include prompt/transcript/summary/manifest/candidate metadata/compressed frames and exclude raw sources, video payloads, and full-resolution frames.
- `SYN_UI_SHOW_VIDEO_EDITOR=1 SYN_UI_VIDEO_EDITOR_RECORDING="$PWD/build/fixture-packets/fixture-1780550940-146CE238-4422-476B-BA65-AC429EB1EEDA/raw/recording-source.mp4" ./script/capture_syn_ui.sh build/ui-captures/syn-compact-zip-action-2026-06-04.png` passed on June 4, 2026. The photograb shows `Create Compact Zip` in the packet detail action row; the status sidecar reports Microphone, Screen Recording, and Accessibility all granted.
- The latest smoke output included `SYN_VIDEO_TRIM_FIXTURE=passed`, `SYN_VIDEO_TRIM_DURATION=3.000`, and `SYN_VIDEO_TRIM_MANIFEST=updated`, proving the app action exports `recording-edited.mp4` and records it in `manifest.json`.
- `SYN_UI_SHOW_VIDEO_EDITOR=1 SYN_UI_VIDEO_EDITOR_RECORDING="$PWD/build/fixture-packets/fixture-1780548940-BE3AAB02-28B6-4686-977E-783B1BC8A9E2/raw/recording-source.mp4" ./script/capture_syn_ui.sh build/ui-captures/syn-video-edit-panel-final-2026-06-04.png` passed on June 4, 2026. The photograb shows the packet detail video editor with start/end inputs and `Save Trimmed Copy`; the status sidecar reports Microphone, Screen Recording, and Accessibility all granted.
- The latest smoke output included `SYN_CAPTURE_PICKER_MODE_TITLES=Screen,All Screens,Chrome Tab,Active Window,Select Window,Region,Smart Region`, `SYN_SMART_REGION_RENDER_FIXTURE=passed`, `SYN_SMART_REGION_RENDER_SIZE=410x202`, `SYN_SMART_REGION_RENDERED_CLICKS=1`, and `SYN_SMART_REGION_RENDER_INTERVALS=2`.
- `./script/live_capture_fixture.sh smartRegion --duration 1` passed on June 4, 2026. It recorded through the staged signed GUI app and real ScreenCaptureKit/microphone path without AI processing. Packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/live-capture-fixtures/live-smartRegion-fixture-1780549096-BD75BB00-1743-4DF4-BB9B-8B3C4E967897`; status `partial`; verification `passed`; `raw/capture-session.json` records `mode=smartRegion`, full-display `outputSize=6144x2560`, and `smartRegion` rectangle `x=1536 y=640 width=3072 height=1280`.
- `SYN_UI_SHOW_PICKER=1 ./script/capture_syn_ui.sh build/ui-captures/syn-capture-picker-smart-region-verified-2026-06-04.png` passed on June 4, 2026. The photograb shows the seven-mode picker including Smart Region; the status sidecar reports Microphone, Screen Recording, and Accessibility all granted.
- `./script/smoke_packet_fixture.sh` passed with a synthetic input recording on June 4, 2026.
- `./script/smoke_packet_fixture.sh` passed again on June 4, 2026 after drawing/annotation overlay verification was added. Latest successful packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780546761-C9D2CD4B-50B6-4F6C-816E-E3F5AF8547A1`; partial failure packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/partial-failure-fixture-1780546805-AAE07CF7-9045-4064-8FD4-252D6DF90D0E`; paused packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/paused-fixture-1780546805-5D02B194-35A9-48DD-9F51-129C9BAC9DDD`; OCR fixture packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/ocr-fixture-1780546761-0BA87065-6280-4F34-A9D7-5F26BF3567B8`. The smoke output included `SYN_ANNOTATION_RECORDER_FIXTURE=passed`, `SYN_ANNOTATION_RECORDER_TOOLS=rectangle,arrow,pen`, `SYN_ANNOTATION_RECORDER_PAUSED_IGNORED=yes`, `SYN_ANNOTATION_RECORDER_CLEAR=passed`, `SYN_ANNOTATION_RENDER_FIXTURE=passed`, `SYN_ANNOTATION_RENDERED_COUNT=3`, `SYN_ANNOTATION_MAPPED_COUNT=3`, and `SYN_ANNOTATION_COLOR_PIXELS=16471`.
- The latest annotation packet manifest records `annotationCount=3`, `annotationMapping.mappedStrokeCount=3`, `annotationMapping.renderedStrokeCount=3`, and `raw/annotations.json` contains rectangle, arrow, and pen strokes with final video coordinates. `agent-prompt.md` references `raw/annotations.json` and includes `Annotations: 3 mapped, 0 unmapped, 3 drawn overlays rendered`.
- `SYN_UI_SHOW_HUD=1 SYN_UI_WINDOW_TITLE="Syn Recording" ./script/capture_syn_ui.sh build/ui-captures/syn-recording-hud-annotations-current-2026-06-04.png` passed on June 4, 2026. The photograb shows the recording HUD with pen, rectangle, arrow, clear, pause, and stop controls; the status sidecar reports Microphone, Screen Recording, and Accessibility all granted.
- `./script/smoke_packet_fixture.sh` passed again on June 4, 2026 after the OCR metadata implementation. Latest successful packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780543533-7E788D2E-4477-46B0-866C-9921DE1F7219`; partial failure packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/partial-failure-fixture-1780543577-17B77C18-E861-4BFB-9445-D19384B20905`; paused packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/paused-fixture-1780543577-1FF43727-61FF-4BEC-BBC7-D6A9A954F446`; OCR fixture packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/ocr-fixture-1780543532-86C2C2F8-D56D-4961-BBE2-D491F6E440E9`. The smoke output included `SYN_CAPTURE_PICKER_MODE_TITLES=Screen,All Screens,Chrome Tab,Active Window,Select Window,Region`, `SYN_PROMPT_PROFILE_FIXTURE=passed`, `SYN_PROMPT_PROFILE_COUNT=3`, `SYN_PROMPT_PROFILE_PERSISTED=qaBugReport`, `SYN_CHROME_TAB_FIXTURE=passed`, `SYN_CHROME_TAB_METADATA=passed`, `SYN_RAW_ZIP_FIXTURE=passed`, `SYN_RAW_ZIP_DEFAULT_EXCLUDES_RAW=yes`, `SYN_RAW_ZIP_INCLUDES_RAW=yes`, `SYN_OCR_FIXTURE=passed`, `SYN_OCR_TEXT=SYN OCR / 4829 / SYN OCR / 4829`, `SYN_ALL_SCREENS_RENDER_FIXTURE=passed`, `SYN_ALL_SCREENS_COMPOSITE_SIZE=2560x720`, and `SYN_ALL_SCREENS_RENDERED_CLICKS=1`.
- The OCR fixture passed on June 4, 2026. It creates a deterministic local text video, runs the real `FrameExtractor`, and verifies `frames/candidates/metadata.json` contains local Vision OCR text, mean confidence, and line bounding boxes. The latest OCR metadata contains two sampled candidates, four text observations, and recognized text `SYN OCR` plus `4829`.
- The prompt profile fixture passed on June 4, 2026. It verifies three built-in profiles, distinct profile files, nonempty profile instructions, and isolated preference persistence for the default profile. The latest smoke packet includes `agent-prompts/general-coding.md`, `agent-prompts/implementation-plan.md`, and `agent-prompts/qa-bug-report.md`; `manifest.json` records `agentPromptProfile=generalCoding` and `files.agentPrompts=agent-prompts`. Photograb: `build/ui-captures/syn-settings-prompt-profiles-2026-06-04.png` shows the Settings profile picker.
- The raw-inclusive zip fixture passed on June 4, 2026. It verifies the default packet zip still excludes `raw/`, the separate `-with-raw.zip` includes raw recording, pointer, and segment files, the History/AppState action updates `manifest.json`, and packet deletion removes the raw zip. Photograb: `build/ui-captures/syn-main-raw-zip-action-2026-06-04.png` shows `Create Raw Zip` in the packet detail action row.
- The Chrome Tab deterministic fixture passed on June 4, 2026. It verifies Chrome tab-list parsing, selected-tab metadata encoding, capture metadata round-tripping, URL note storage, and the picker contract. Photograbs: `build/ui-captures/syn-capture-picker-smart-region-verified-2026-06-04.png` shows Chrome Tab in the current seven-mode picker; `build/ui-captures/syn-chrome-tab-selector-fixture-final-2026-06-04.png` shows the Chrome tab selector.
- `build/ui-captures/syn-main-after-chrome-tab-smoke-2026-06-04.png` photograbs the normal staged app after the Chrome Tab smoke. The captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `build/ui-captures/syn-main-after-ocr-smoke-final-2026-06-04.png` photograbs the normal staged app after the final OCR smoke. The captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `build/ui-captures/syn-main-after-full-smoke-hotkey-all-screens-2026-06-04.png` photograbs the normal staged app after the full smoke and hotkey/all-screens checks; the captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `docs/IMPLEMENTATION_AUDIT.md` records the requirement-by-requirement MVP coverage audit against the source spec and corroborated decisions.
- `./script/build_and_run.sh --verify` built and launched the debug app on June 3, 2026.
- `./script/hotkey_fixture.sh` passed on June 4, 2026 after tightening the picker/repeat race again. The left/right Shift event tap, picker chord, repeat chord, and chord disambiguation logic all registered/passed with status `0`.
- The hotkey fixture now verifies the `Left Shift + Right Shift` / `Left Shift + Right Shift + R` ambiguity directly: plain two-Shift release queues repeat through a short suffix grace plus input-drain pass, `R` during the armed or pending chord cancels repeat and opens the picker, and late unrelated `R` no longer steals repeat. The fixture includes a short two-Shift repeat regression, quick and late suffix-gap cases, an out-of-order suffix case, an `R` key-up-only case while repeat is pending, a case where `R` is already down before the second Shift, a duplicated modifier-event case, a missing-R-event case where physical R-key polling must open the picker before repeat wins, a long-held missing-R-event case, a pending-repeat physical-R case, and a repeat-deadline input-drain case where queued `R` still beats repeat.
- The live hotkey listener now uses listen-only HID, session, and annotated-session key taps, an AppKit R-key monitor, physical R-key polling while the two-Shift chord is armed and during the pending-repeat grace window, sticky R participation for the current chord lifecycle, and a short input-drain pass before repeat fires. This fixed reproduced live held-R failures where macOS did not deliver a separate `R` event before repeat won and reduces the timer-vs-R event ordering race.
- `./script/live_hotkey_fixture.sh suffix-r` passed on June 4, 2026. It launched the signed staged GUI app, posted the ambiguous `Left Shift + Right Shift`, release, then quick `R` sequence into the live macOS event stream, logged the picker callback, wrote a hotkey event trace, and photograbbed `build/ui-captures/syn-live-hotkey-suffix-r.png` showing the picker. The latest trace shows `pending repeat generation=4`, `poll r generation=4`, and `action picker`.
- `./script/live_hotkey_fixture.sh slow-suffix-r` passed on June 4, 2026. It verifies a late `R` after the suffix grace no longer steals repeat; the latest event trace shows `pending repeat input drain generation=4` and `action repeat generation=4`, and the photograb shows the overview/repeat path instead of the picker.
- `./script/live_hotkey_fixture.sh held-r` passed on June 4, 2026 after reproducing the picker-lost race once. The failing trace saw only Shift events and logged `repeat`; after the armed-chord R poll fix, the latest trace shows `poll chord r generation=3` then `action picker`, and `build/ui-captures/syn-live-hotkey-held-r.png` shows the picker.
- `./script/live_hotkey_fixture.sh fast-held-r` passed on June 4, 2026 after reproducing a second picker-lost race where a 1 ms R tap while both Shifts were held was invisible to macOS event delivery and logged `repeat`. After the armed-chord physical R poll fix, the trace shows `poll chord r generation=3` then `action picker`, and `build/ui-captures/syn-live-hotkey-fast-held-r.png` shows the picker.
- `./script/live_hotkey_fixture.sh long-held-r` passed on June 4, 2026 after the annotated-session tap and stricter late-repeat guard. It verifies the slower human path where both Shifts are held longer before `R` is tapped, waits out the repeat window, requires exactly one action, and photograbs `build/ui-captures/syn-live-hotkey-long-held-r.png` showing the picker. The latest trace shows `poll chord r generation=3` then `action picker`; the latest action log contains exactly `picker`.
- `./script/live_hotkey_fixture.sh repeat` passed on June 4, 2026 after the annotated-session tap and stricter late-repeat guard. It launched the signed staged GUI app in hotkey observer mode, posted the plain `Left Shift + Right Shift` sequence, logged exactly one repeat callback without starting a recording, wrote a hotkey event trace, and photograbbed `build/ui-captures/syn-live-hotkey-repeat.png`. The latest repeat trace includes `pending repeat input drain generation=4` before `action repeat generation=4`.
- `SYN_HOTKEY_SEQUENCE=held-r ./script/live_hotkey_recording_fixture.sh picker selectedWindow 1.0` passed on June 4, 2026 after the repeat suffix-grace change. The shortcut action log recorded `picker`, the event trace shows the picker winning via `poll chord r generation=3`, the HUD photograb is `build/ui-captures/syn-live-hotkey-recording-picker-selectedWindow-held-r-hud.png`, and the raw packet is `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-03-27-56`.
- `SYN_HOTKEY_SEQUENCE=fast-held-r ./script/live_hotkey_recording_fixture.sh picker selectedWindow 1.5` passed on June 4, 2026. The shortcut action log recorded `picker`, the event trace shows the picker winning via `poll chord r generation=3`, the HUD photograb is `build/ui-captures/syn-live-hotkey-recording-picker-selectedWindow-fast-held-r-hud.png`, and the raw packet is `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-02-58-22`.
- `SYN_HOTKEY_SEQUENCE=long-held-r ./script/live_hotkey_recording_fixture.sh picker selectedWindow 1.0` passed again on June 4, 2026 after the annotated-session tap. The shortcut action log recorded `picker`, the event trace shows the picker winning via `poll chord r generation=3`, and the raw packet is `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-05-37-11`. The follow-up main-window photograb `build/ui-captures/syn-main-after-hotkey-annotated-tap-2026-06-04.png` shows the resulting selected-window packet and all permissions allowed.
- `./script/live_hotkey_recording_fixture.sh repeat selectedWindow 1.0` passed on June 4, 2026 after the repeat suffix-grace change. The shortcut action log recorded `repeat`, the HUD photograb is `build/ui-captures/syn-live-hotkey-recording-repeat-selectedWindow-repeat-hud.png`, and the raw packet is `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-03-28-21`.
- `build/ui-captures/syn-main-after-hotkey-annotated-tap-2026-06-04.png` photograbs the normal staged app after the latest hotkey regression checks; it shows Screen Recording, Microphone, and Accessibility all allowed.
- The capture-picker contract fixture passed on June 4, 2026. It verifies the picker modes are exactly `Screen`, `All Screens`, `Chrome Tab`, `Active Window`, `Select Window`, and `Region`, and that the picker contract includes mic status, last mode, and Settings affordances.
- The repeat-last policy fixture passed on June 3, 2026; repeat-last opens the picker until at least one packet has completed as `Succeeded` or `Partial`.
- `./script/live_capture_fixture.sh --duration 1` passed on June 3, 2026, using the staged signed app bundle to capture a local raw ScreenCaptureKit screen+microphone recording without AI processing.
- `./script/live_capture_fixture.sh activeWindowFollow --duration 1` passed on June 3, 2026, using the staged signed app bundle to capture real active-window-follow raw screen+microphone recording without AI processing.
- `./script/live_capture_fixture.sh selectedWindow --duration 1` passed on June 3, 2026, using the staged signed app bundle to capture a real selected-window raw ScreenCaptureKit recording without AI processing.
- `./script/live_capture_fixture.sh region --duration 1` passed on June 3, 2026, using the staged signed app bundle to capture a real cropped-region raw ScreenCaptureKit recording without AI processing.
- `./script/live_capture_fixture.sh region --duration 1` passed again on June 4, 2026 after the live fixture script was updated to keep the display session awake. Without that assertion, ScreenCaptureKit can report no displays when the session has gone idle.
- `./script/live_capture_fixture.sh screen --duration 1`, `allScreens --duration 1`, `activeWindowFollow --duration 1`, `selectedWindow --duration 1`, and `region --duration 1` all passed on June 4, 2026 after the live fixture was strengthened to verify raw packet files and H.264/AAC streams. Packets include `build/live-capture-fixtures/live-screen-fixture-1780535587-36B87589-2098-4B86-9557-470B19CC674F`, `build/live-capture-fixtures/live-allScreens-fixture-1780538681-16264153-7E19-42E6-8C19-707CD6034D48`, `build/live-capture-fixtures/live-activeWindowFollow-fixture-1780535594-AA4FEC1C-4CE4-4FE0-A8E3-C4A7501E1830`, `build/live-capture-fixtures/live-selectedWindow-fixture-1780535601-C5A6CC6A-7101-478B-B96A-DCB7AFD2A5A5`, and `build/live-capture-fixtures/live-region-fixture-1780535607-636A8BC7-884F-4DDE-9EC3-DD89243CABFE`. The latest all-screens packet recorded 3 display streams into a 3840x946 H.264/AAC raw virtual desktop canvas and verified that capture metadata matches the raw video dimensions.
- `build/ui-captures/syn-main-after-live-raw-modes-2026-06-04.png` photograbs the normal staged app after the current live raw mode verification; the captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `SYN_UI_SHOW_REGION_SELECTOR=1 ./script/capture_syn_ui.sh build/ui-captures/syn-region-selector-fixture.png` passed on June 4, 2026. The photograb shows the region overlay with a selected rectangle, dimensions, and Confirm/Cancel controls; the captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `SYN_UI_SHOW_WINDOW_SELECTOR=1 ./script/capture_syn_ui.sh build/ui-captures/syn-window-selector-fixture.png` passed on June 4, 2026. The helper starts a temporary external target window, and the photograb shows the selected-window overlay with a highlighted/preselected target and confirmation controls; the captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `./script/selector_confirm_fixture.sh region` passed on June 4, 2026. It launches the region selector in observer mode, auto-confirms the selected rectangle through the selector controller, and verifies the app-state callback logs a `region` confirmation without starting a real recording.
- `./script/selector_confirm_fixture.sh selectedWindow` passed on June 4, 2026. It launches the selected-window selector with a temporary external target, auto-confirms the selected target through the selector controller, and verifies the app-state callback logs a `selectedWindow` confirmation without starting a real recording.
- `./script/selector_input_fixture.sh region` passed again on June 4, 2026 after draggable region refinement was added. It launches the actual region overlay without auto-confirm, photograbs `build/ui-captures/syn-selector-input-region-before.png`, drives the selector view through synthetic in-app `NSEvent` `mouseDown`, `mouseDragged`, `mouseUp`, then drags the selected rectangle before Return. The app callback logged `region 2908,1172,520,360 display=4 moved=true`.
- `./script/selector_input_fixture.sh selectedWindow` passed on June 4, 2026. It launches the actual selected-window overlay without preselect/auto-confirm, photograbs `build/ui-captures/syn-selector-input-selectedWindow-before.png`, then drives hover/click selection plus Return confirmation against a temporary external target window. The app callback logged `selectedWindow 8291`.
- `./script/selector_recording_fixture.sh region 1.0` passed on June 4, 2026. It auto-confirms the actual region selector, starts a real ScreenCaptureKit recording through the staged signed GUI app, stops after about 1 second, and writes a local raw `partial` packet without AI processing: `/Users/trmd/Movies/Syn/2026-06-04/region-recording-01-08-24`.
- `./script/selector_recording_fixture.sh selectedWindow 1.0` passed on June 4, 2026. It starts a temporary external target window, auto-confirms the actual selected-window selector, records for about 1 second, and writes a local raw `partial` packet without AI processing: `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-01-08-44`.
- `./script/selector_recording_fixture.sh selectedWindow 1.0 --process` passed on June 4, 2026. It started a temporary external target window, auto-confirmed the actual selected-window selector, recorded through the staged signed GUI app, processed the packet, and verified the completed artifact files. Packet: `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-01-42-19`; status `succeeded`; providers recorded in `manifest.json`: bundled Whisper `ggml-base.en.bin`, OpenAI `gpt-5-mini`, and Anthropic `claude-opus-4-8`.
- `./script/selector_recording_fixture.sh region 1.0 --process` passed on June 4, 2026. It opened a temporary safe fixture window under the centered region selector, auto-confirmed the actual region selector, recorded through the staged signed GUI app, processed the packet, and verified the completed artifact files. Packet: `/Users/trmd/Movies/Syn/2026-06-04/region-recording-01-43-16`; status `succeeded`; providers recorded in `manifest.json`: bundled Whisper `ggml-base.en.bin`, OpenAI `gpt-5-mini`, and Anthropic `claude-opus-4-8`.
- `./script/selector_recording_fixture.sh region 1.0 --process` passed again on June 4, 2026 after strengthening the selector verifier. Packet: `/Users/trmd/Movies/Syn/2026-06-04/region-recording-03-34-39`; status `succeeded`. The verifier now checks raw H.264/AAC capture, raw capture-session mode, processed 30 FPS H.264/AAC output, bundled Whisper `ggml-base.en.bin`, OpenAI `gpt-5-mini`, Anthropic `claude-opus-4-8`, selected frame file metadata, agent-prompt sections, and default zip selected-frame inclusion while excluding `raw/`.
- `./script/selector_recording_fixture.sh selectedWindow 1.0 --process` passed again on June 4, 2026 after strengthening the selector verifier. Packet: `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-03-35-23`; status `succeeded`; zip: `/Users/trmd/Movies/Syn/2026-06-04/select-window-recording-03-35-23.zip`.
- `build/ui-captures/syn-main-after-processed-selector-current-2026-06-04.png` photograbs the normal staged app after the strengthened processed selector runs. It shows the latest selected-window packet as `Succeeded`, exposes `Reveal Zip`, and reports Microphone, Screen Recording, and Accessibility all granted.
- Fresh selector photograbs after the selector-recording fixtures passed on June 4, 2026: `build/ui-captures/syn-region-selector-after-recording-fixture.png` shows the selected rectangle, dimensions, Confirm, and Cancel; `build/ui-captures/syn-window-selector-after-recording-fixture.png` shows the highlighted selected-window target and confirmation controls.
- Fresh selector photograbs after the processed selector fixture work passed on June 4, 2026: `build/ui-captures/syn-region-selector-after-processed-fixture.png` shows the centered region selection with dimensions plus Confirm/Cancel, and `build/ui-captures/syn-window-selector-after-processed-fixture.png` shows the selected-window overlay with the highlighted target and confirmation controls.
- The fixture packet completed with `processing.status = succeeded`.
- The fixture smoke now verifies processed, paused, and retried `recording.mp4` media contracts with `ffprobe`: H.264 video, nominal 30 FPS, valid dimensions, and AAC audio. The latest checked packet was `build/fixture-packets/fixture-1780527110-A79D6865-5D48-4B7E-9AA2-352BE8B09B97/recording.mp4`, which probed as H.264 1280x720 at `30/1` FPS with AAC mono audio.
- The fixture processing path used bundled local Whisper, OpenAI semantic frame planning, and Claude Opus summary generation.
- The summary contract fixture verifies Claude's actual request payload includes one text block with the transcript plus rich selected-frame metadata, and selected compressed JPEG image blocks encoded as base64 `image/jpeg`. It also verifies raw audio/video paths are not attached or referenced as attachments.
- New packet manifests now record both AI model identifiers used in processing: `frameSelectionModel` for OpenAI semantic frame planning and `summaryModel` for Claude summary generation.
- The fixture packet verified processed video rendering, click bubble rendering, pointer event metadata, pointer mapping metadata, selected frame files, normalized pixel-difference metadata, manifest paths, and zip creation containing required packet artifacts while excluding `raw/`.
- Selected frame metadata now records full-frame and compressed-frame dimensions and byte counts; the fixture smoke verifies selected full frames are PNGs, selected compressed frames are JPEGs, compressed frames stay within the 1600 px long-edge LLM-ready profile, and metadata byte counts match disk.
- The fixture smoke now verifies the frame folder contract: `frames/candidates/` contains only `metadata.json` by default, `frames/full/` and `frames/compressed/` exactly match selected frame metadata, and the default zip includes the selected PNG/JPEG frame files while still excluding `raw/`.
- The fixture smoke now verifies opt-in candidate debug retention: with `SYN_KEEP_CANDIDATE_FRAMES=1`, `frames/candidates/` stores a JPEG for every sampled candidate and metadata records `candidatePath`, `candidateSize`, and `candidateBytes`.
- The fixture smoke now verifies the ScreenCaptureKit capture configuration contract: cursor rendering is enabled, ScreenCaptureKit's built-in click marks are disabled so Syn can burn its own click bubbles, microphone capture is enabled, system audio is disabled, current-process audio is excluded, and Syn-owned windows are excluded from display capture.
- The recording HUD photograb path passed on June 3, 2026: `SYN_UI_SHOW_HUD=1 SYN_UI_WINDOW_TITLE="Syn Recording" ./script/capture_syn_ui.sh build/ui-captures/syn-recording-hud-contract.png` captured the actual HUD panel by title. The screenshot shows the mode label, elapsed timer, mic meter, pause/resume button, and stop button.
- The fixture smoke verifies `agent-prompt.md` names the packet folder and shareable zip, embeds agent workflow instructions, capture/processing metadata, processing caveats, pointer/pause metadata, selected full/compressed frame references, summary content, and a transcript excerpt. It also verifies the macOS pasteboard contains the generated prompt as text plus the packet folder as a file URL after packet processing.
- The fixture smoke verifies History `Copy Packet`, missing-zip hiding, and `Delete` behavior against an isolated history store: packet copying updates the pasteboard/status with both prompt text and packet folder file URL, missing zip paths are not exposed as available zips, and deletion removes the packet folder, adjacent zip, selection, and history row without touching the real history file.
- The fixture smoke verifies the default packet layout contract: `~/Movies/Syn/YYYY-MM-DD/<slug>-<timestamp>/` with a sibling `<slug>-<timestamp>.zip`.
- The fixture smoke verifies the Keychain-backed secret store using an isolated fixture service/account: empty read, status-aware save/read, status-aware overwrite, and status-aware delete all pass without printing secret values or touching the real Anthropic/OpenAI accounts.
- The fixture smoke now also verifies interrupted history recovery and retry: raw packets and segment-only raw packets become `partial`/retryable; empty interrupted packets become `failed`; segment-only retry produces a full packet.
- The fixture smoke verifies the 30-minute warning threshold without imposing a hard recording limit.
- The fixture smoke verifies the local fallback summary contract, so `summary.md` still has the required coding-agent sections when Claude is unavailable.
- The fixture smoke verifies active-window-follow rendering creates a padded fixed canvas and burns in a click bubble from active-window pointer metadata.
- The fixture smoke verifies pause/resume packet processing stores one pause interval and keeps the final processed duration to the recorded segments, excluding paused time.
- The fixture smoke verifies visual-change metadata includes active app/window title, capture bounds, perceptual hash, normalized pixel difference, and local Vision OCR text/observations, while normal screen captures are not mislabeled as active-window timeline renders.
- Repeat-last capture settings now persist after a successful completed recording; selected-window repeat stores window owner/app metadata and reopens the selector if the remembered window is no longer available or no longer matches the stored target.
- Selected-window mode now uses select/highlight/confirm semantics; `./script/build_and_run.sh --verify` passes after the overlay change.
- Syn now launches from stable `~/Applications/Syn.app` instead of the moving DerivedData app path.
- Syn stays configured as a menu bar/accessory utility; the staged bundle has `LSUIElement=true`, and the main window controller no longer switches the app to regular Dock activation.
- Syn debug builds now prefer Apple Development signing with `TeamIdentifier=4QK8JBAU4V`; `Rift Local Signing` is only a fallback because it has no Team ID and can make macOS privacy grants appear to disappear across rebuilds.
- The Permissions checklist now has direct `Request` actions for Screen Recording, Microphone, and Accessibility where macOS supports prompts.
- The Permissions checklist refreshes when the window appears, not only from the Refresh button or app activation.
- The startup permission gate now treats Accessibility as optional metadata support; Screen Recording and Microphone are the required recording blockers.
- Settings now shows whether the picker and repeat global hotkeys are registered or failed.
- Global capture shortcuts now avoid the old `Command-Shift-R` / `Command-Option-R` chords: `Left Shift + Right Shift + R` opens the picker, and `Left Shift + Right Shift` with no other key repeats the last capture mode after a short suffix grace/input-drain pass.
- Hotkey resolution now runs on a dedicated HID event-tap thread and derives left/right Shift state from each event's modifier bits when macOS provides them, falling back to the ordered event stream only when those bits are unavailable. Repeat waits through a short suffix grace so the picker chord has priority when `R` follows the two Shift keys as part of the same gesture, then drains pending input before firing. Any `R` key-down/up seen while the shift chord is armed or repeat is pending resolves to picker; Syn also polls R while the chord is armed and while repeat is pending so picker still wins if key state is visible but macOS does not deliver a separate event.
- Settings now shows redacted Anthropic/OpenAI key availability and source (`Keychain`, `hem`, or missing) without exposing the key values.
- Settings now has explicit `Save Keys to Keychain` and `Clear Saved Keys` actions for Anthropic/OpenAI keys, and surfaces Keychain operation failures with the underlying `OSStatus` instead of assuming success.
- Main-window, picker, and menu-bar Settings affordances now use Syn's own AppKit-hosted Settings window instead of SwiftUI `SettingsLink`, so they use the same verified Settings path as `SYN_UI_SHOW_SETTINGS=1`.
- History rows now show packet status, created time, and duration.
- Successful processing/retry status now says the packet is ready, the agent prompt and packet folder were copied, and the folder was revealed.
- Successful processing/retry and History `Copy Packet` now copy both the agent prompt text and the packet folder file URL to the macOS pasteboard.
- Packet output locations are now keyboard/menu accessible: `Command + Shift + O` opens the selected/latest packet folder, `Command + Shift + C` copies the selected/latest packet handoff, and `Command + Option + O` reveals the selected/latest packet zip when available. The same actions are also available from the menu bar item.
- Packet detail only shows `Reveal Zip` when the zip file exists on disk; partial packets with missing zips keep retry/delete actions without offering a dead reveal button.
- The build script now preserves the staged `/Users/trmd/Applications/Syn.app` bundle directory when updating builds, syncs stale files out of `Contents`, and registers the app with Launch Services. This should make macOS privacy settings track one stable app path instead of a recreated bundle.
- The Permissions checklist now has a `Relaunch Syn` button when Screen Recording is not granted, because macOS does not let the current process observe a new Screen Recording grant through Refresh alone.
- The Permissions checklist now has a `Reset Mic` action for a denied/stuck Microphone grant. It resets only Syn's Microphone TCC row, relaunches Syn, and automatically requests Microphone on the next launch.
- `./script/reset_microphone_permission.sh` resets only Microphone permission for `com.trmd.syn`; it does not reset Screen Recording or Accessibility.
- `./script/permission_status.sh` prints Syn's own read-only view of Microphone, Screen Recording, and Accessibility status from the staged app bundle, including both `AVAudioApplication.recordPermission` and `AVCaptureDevice.authorizationStatus`.
- `./script/capture_syn_ui.sh` launches the staged app with the requested main, picker, HUD, or Settings window visible, keeps the display session awake during capture, captures the actual Syn window, falls back to a full-screen crop if macOS refuses direct window capture, and writes the GUI process permission status next to the screenshot. Use this for permission UI verification instead of relying on direct CLI fixtures alone.
- Serial photograbs after the Settings-affordance change passed on June 3, 2026: `build/ui-captures/syn-main-after-settings-action.png`, `build/ui-captures/syn-picker-after-settings-action.png`, and `build/ui-captures/syn-settings-after-settings-action.png`.
- `build/ui-captures/syn-main-after-zip-availability.png` shows a selected partial packet whose missing zip is no longer exposed as a `Reveal Zip` action.
- `build/ui-captures/syn-main-after-packet-clipboard.png` shows the packet detail action renamed to `Copy Packet` after the clipboard handoff started copying both prompt text and packet folder file URL.
- `build/ui-captures/syn-main-after-packet-commands.png` shows the main packet detail after adding packet folder/copy/reveal commands; the captured GUI process reports all permissions granted.
- `SYN_UI_SHOW_PICKER=1 ./script/capture_syn_ui.sh build/ui-captures/syn-capture-picker-chrome-tab-2026-06-04.png` photograbs the actual capture picker. The latest picker screenshot shows all six capture modes, `Mic ready`, last mode, and the Settings entry.
- `build/ui-captures/syn-picker-after-hotkey-event-thread.png` photograbs the actual capture picker after moving hotkey resolution onto its own event thread.
- `build/ui-captures/syn-picker-after-hotkey-thread-final.png` is the latest picker photograb after the final hotkey race fix; the captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `build/ui-captures/syn-picker-after-hotkey-event-flags.png` photograbs the actual capture picker after deriving left/right Shift state from macOS event modifier bits; the captured GUI process reports Microphone, Screen Recording, and Accessibility all granted.
- `build/ui-captures/syn-live-hotkey-long-held-r.png` is the latest live-hotkey picker photograb after hardening the live listener; it shows the picker opening from the long-held shortcut path with `Mic ready`.
- `SYN_UI_SHOW_SETTINGS=1 ./script/capture_syn_ui.sh build/ui-captures/syn-settings-keychain.png` photograbs the actual Settings window. The latest screenshot shows hotkey registration, output folder, redacted Anthropic/OpenAI key availability, secure key fields, and Save/Clear Keychain actions.
- `SYN_UI_WINDOW_TITLE="Syn Recording"` can target the floating HUD window specifically, instead of accidentally capturing the main Syn window.
- Live shortcut verification on June 4, 2026: `build/ui-captures/syn-live-hotkey-suffix-r.png`, `build/ui-captures/syn-live-hotkey-held-r.png`, `build/ui-captures/syn-live-hotkey-fast-held-r.png`, and `build/ui-captures/syn-live-hotkey-long-held-r.png` show the picker winning for picker shortcut variants; `build/ui-captures/syn-live-hotkey-slow-suffix-r.png` and `build/ui-captures/syn-live-hotkey-repeat.png` show `Left Shift + Right Shift` resolving to repeat when `R` is too late or absent.
- `./script/smoke_packet_fixture.sh` passed again on June 4, 2026 after strengthening the enriched `agent-prompt.md` contract. Latest packet: `/Users/trmd/Projects/trmd/syn/repos/syn/build/fixture-packets/fixture-1780532956-2BE3D5FE-5853-4D04-B679-901941111E25`.
- `build/ui-captures/syn-main-after-agent-prompt-contract.png` photograbs the actual staged Syn main window after the enriched prompt work; it shows Screen Recording, Microphone, and Accessibility all allowed.
- Current staged app status on June 3, 2026: `SYN_PERMISSION_MICROPHONE=authorized`, `SYN_PERMISSION_MICROPHONE_RECORD=granted`, `SYN_PERMISSION_MICROPHONE_CAPTURE_DEVICE=authorized`, `SYN_PERMISSION_SCREEN=granted`, and `SYN_PERMISSION_ACCESSIBILITY=granted`.
- Current staged app visual verification on June 3, 2026: `build/ui-captures/syn-after-media-contract-smoke.png` shows Screen Recording, Microphone, and Accessibility as `Allowed`.
- Syn now has the required hardened runtime microphone entitlement: `com.apple.security.device.audio-input=true` in `Syn/Syn.entitlements`, wired through `CODE_SIGN_ENTITLEMENTS=Syn/Syn.entitlements`.
- Tormod reported that permissions now work after the stable app/signing changes.
- The live raw fixtures produced `raw/recording-source.mp4` with H.264 video and AAC audio, plus `raw/capture-session.json` and active-window sample metadata across screen, active-window-follow, selected-window, and region modes.
- ScreenCaptureKit recording start/stop is now timeout-bounded. If `SCRecordingOutput` never calls its start or finish delegate, Syn reports `Screen recording did not start` / `Screen recording did not stop` instead of waiting indefinitely. The selector-recording fixture logs such failures as `status=failed` with the error text.
- The interrupted real `Region recording` packet at `/Users/trmd/Movies/Syn/2026-06-03/region-recording-17-33-56/` was recovered to `partial` from its raw segment file.

## Remaining Physical Checks

The remaining checks are optional manual confidence checks because the automated live fixtures post events into macOS, but they do not physically press the keyboard or trackpad:

1. Press `Left Shift + Right Shift + R` with R held before releasing Shift, record a short real packet, stop it, and confirm History shows `Succeeded` or `Partial`.
2. Press and release `Left Shift + Right Shift` after one successful packet, without pressing another key, and confirm it repeats the last capture mode.
3. Start `Select Window`, click a highlighted window manually, and confirm it. The selected-window view event handlers and selector-to-processed-packet path are already automated; this only verifies physical hardware/OS event delivery.
4. Start `Region`, drag a rectangle manually, and confirm it. The region view event handlers and selector-to-processed-packet path are already automated; this only verifies physical hardware/OS event delivery.
5. Start `Smart Region`, drag an initial rectangle manually, move the cursor around while recording, stop, and confirm the processed `recording.mp4` follows the cursor. The render fixture and raw live capture path are automated; this only verifies the manual selection feel and real cursor-following usefulness.
6. During a short safe recording, use the HUD pen, rectangle, and arrow tools to draw on screen, stop, and confirm `recording.mp4` contains the drawings. Recorder metadata and burn-in rendering are automated; this only verifies physical drawing feel and OS event delivery.
7. If click/mouse metadata is incomplete, grant Accessibility to `/Users/trmd/Applications/Syn.app` and retry a short recording.
8. Retry the recovered partial `Region recording` from History if that recording's contents are safe to send through the configured AI providers.

If Syn still says a permission is missing after granting it:

1. Use `Relaunch Syn` in the Permissions section. For Screen Recording, `Refresh` cannot see a new grant until Syn restarts.
2. If System Settings has removed Syn from the permission list, quit Syn completely from the menu bar item.
3. Run `./script/build_and_run.sh --verify`.
4. Reopen the Syn window and press Refresh in the Permissions section.

If only Microphone is stuck as `Needed` and System Settings will not let you remove Syn from the list:

1. Click `Reset Mic` next to Microphone in Syn's Permissions section.
2. Syn should relaunch and immediately show the macOS Microphone prompt.
3. Approve the prompt, then press Refresh if the row has not updated yet.

If no prompt appears, run the read-only status command:

```bash
./script/permission_status.sh
```

If it reports `SYN_PERMISSION_MICROPHONE=denied`, run `./script/reset_microphone_permission.sh`, then `./script/build_and_run.sh --verify`, open Syn's Permissions section, and click `Reset Mic` once more.

If Syn's UI appears to disagree with the CLI status, capture the actual app window and GUI process status:

```bash
./script/capture_syn_ui.sh
```

If the screenshot/status still says Microphone is `Needed` without any macOS prompt, inspect the signed entitlement:

```bash
codesign -dvvv --entitlements :- ~/Applications/Syn.app
```

The entitlements output must include `com.apple.security.device.audio-input`. Without that entitlement, hardened runtime prevents the Microphone prompt from appearing and TCC denies `kTCCServiceMicrophone`.

If it still looks wrong, run the read-only diagnostic script and share the output:

```bash
./script/diagnose_permissions.sh
```

The diagnostic should show:

- Expected bundle: `/Users/trmd/Applications/Syn.app`
- Expected bundle id: `com.trmd.syn`
- A running process whose path is `/Users/trmd/Applications/Syn.app/Contents/MacOS/Syn`
- Authority: `Apple Development: Tormod Haugland (QT5J6P28AM)`
- TeamIdentifier: `4QK8JBAU4V`
- `NSMicrophoneUsageDescription` present in `Info.plist`
- `com.apple.security.device.audio-input` present in the signed entitlements

The build script now prefers Apple Development signing when available. If the diagnostic reports `TeamIdentifier=not set`, `Signature=adhoc`, or `origin=Rift Local Signing`, rebuild with `./script/build_and_run.sh --verify` and regrant permissions once for the Apple Development signed app.

The launched debug app is built with Xcode's debug dylib disabled so the signed bundle can start cleanly under hardened runtime.

## Tormod feedback

- Granted things (except for microphone, I never get the option), but it says in the UI that things are not granted.
- Added in-app bundle path/bundle ID diagnostics and a read-only permission diagnostic script to narrow this down.
- Things worked after the permission/signing fix; crash reports found afterward were from pre-fix builds. The 17:19 report matched the fixed ScreenCaptureKit `SCStream` configuration crash, and the later 17:21 reports matched the fixed debug dylib signing mismatch.
- Permissions appeared gone again after reopening Syn; diagnostics showed `Rift Local Signing` with no Team ID. Build scripts now prefer Apple Development signing and the permissions pane refreshes on appear.
- Clicking Microphone `Request` later went directly to `Needed` / `Reset Mic` without showing a prompt. Unified logs showed hardened runtime denying the prompt because `com.apple.security.device.audio-input` was missing. Adding `Syn/Syn.entitlements` fixed the root cause; the signed staged bundle now includes that entitlement, and the captured Syn UI shows Microphone as `Allowed`.
